<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="content"
            type="org.firespeed.metronome.ui.settings.BpmItem" />
    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="8dp"
        android:foreground="?android:selectableItemBackground"
        android:background="@drawable/item_list_background">
        <TextView
            android:id="@+id/title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:text="@{content.bpm.title}"
            android:textColor="@color/item_list_primary"
            android:textSize="14sp"
            tools:text="hogehoge" />

        <TextView
            android:id="@+id/bpm"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="end"
            android:padding="8sp"
            android:text="@{Integer.toString(content.bpm.bpm)}"
            android:textColor="@color/item_list_primary_dark"
            android:textSize="22sp"
            tools:text="888" />


    </LinearLayout>
</layout>