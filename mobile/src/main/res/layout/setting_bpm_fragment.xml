<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
        <variable name="viewModel"
            type="org.firespeed.metronome.ui.SettingBpmViewModel"/>
        <variable
            name="fragment"
            type="org.firespeed.metronome.ui.settings.SettingBpmFragment" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/setting_bpm"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".ui.settings.SettingBpmFragment">
        <EditText
            android:id="@+id/title"
            android:autofillHints="@string/title"
            android:layout_width="0dp"
            android:hint="@string/title"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            android:layout_height="wrap_content"
            android:maxLines="1"
            android:inputType="text" />
        <EditText
            android:id="@+id/bpm"
            android:inputType="numberDecimal"
            android:maxLines="1"
            android:autofillHints="60"
            android:layout_width="0dp"
            android:hint="@string/bpm"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toStartOf="@id/add"
            app:layout_constraintTop_toBottomOf="@id/title"
            android:layout_height="wrap_content"
            />
        <Button
            android:id="@+id/add"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/add"
            app:layout_constraintTop_toBottomOf="@+id/title"
            app:layout_constraintEnd_toEndOf="parent"
            />
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/bpmList"
            android:layout_width="0dp"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintTop_toBottomOf="@id/bpm"
            android:layout_height="0dp"
            />



    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>